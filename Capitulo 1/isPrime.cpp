#include <bits/stdc++.h>
#define input freopen("in.txt","r",stdin)
#define output freopen("out.txt","w",stdout)
using namespace std; 

// Verificar si un numero era primo o no
bool isPrime() {
    read(number); // leer el numero   // 1
    var contDivisores = 0;            // 1
    var esPrimo = false;              // 1
    
    if(contDivisores == 2) {           // cond 1
        esPrimo = true;                // 1
    }
    else {
        int cont = 0;                 // 1
        cont++;                       // 1
        print(cont);                  // 1
    }
    return esPrimo;                   // 1
}


int main() {

    return 0;
}